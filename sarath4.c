#include<stdio.h>
#include<conio.h>
int main()
{
int a,b,c;
scanf("%d%d%d",&a,b,c);
if(a>b&&a>c)     
printf("%d a is the larger number",a);
else(a>b)  
     
      printf("%d b is the largest number",b);
      
     else
      {
      printf("%d is the largest number",c);
     }
      getch();
      return 0;
      }

