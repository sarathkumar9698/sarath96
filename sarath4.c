#include<stdio.h>
#include<conio.h>
int main()
{
int a;
{
if(a<5);
scanf("%d",&a);
printf(the numbers are small");
else(a=1)
     {
      printf("The no are equal");
      }
      else(a>5)
      {
      printf("the numbers are large");
      }
      }
      getch();
      return 0;
      }

